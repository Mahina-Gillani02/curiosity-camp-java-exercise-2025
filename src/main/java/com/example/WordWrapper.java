package com.example;

public class WordWrapper {

    public String wrap(String text, int columnLength) {
//        GIVEN SOLUTION
//        if (text.length() <= columnLength) {
//            return text;
//        }
//        String wrappedText = text.substring(0, columnLength) + '\n';
//        String remainingText = text.substring(columnLength);
//        return wrappedText + this.wrap(remainingText, columnLength);
        return "";
    }
}
